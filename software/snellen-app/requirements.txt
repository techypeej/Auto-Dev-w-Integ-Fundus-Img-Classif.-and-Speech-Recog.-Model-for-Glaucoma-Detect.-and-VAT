# Install on Linux: pip install -r requirements.txt  (requires: sudo apt-get install portaudio19-dev)
# Install on Windows: run setup.bat instead of pip install directly (pyaudio needs pipwin on Windows)
SpeechRecognition>=3.10.0
pyaudio>=0.2.13
