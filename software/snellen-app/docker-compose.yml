services:
  snellen-app:
    build: .
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix  # X11 display forwarding
      - ./results:/app/results          # persist results on host
    network_mode: host
    stdin_open: true
    tty: true
